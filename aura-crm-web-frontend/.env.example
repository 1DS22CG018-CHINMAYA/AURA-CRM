# Aura CRM – Frontend environment variables
# Copy this to .env (never commit .env to git)

# FastAPI backend base URL — no trailing slash
VITE_API_URL=http://localhost:8000

# MongoDB ObjectId of your user account.
# Create a user via POST /users/ or copy from your test suite output.
VITE_USER_ID=69a29c321244edf98a29e948

# Display name shown in the sidebar
VITE_USER_NAME=Chinmaya Adiga
